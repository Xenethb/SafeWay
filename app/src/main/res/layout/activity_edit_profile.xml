<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Title -->
        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Edit Profile"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="24dp" />

        <!-- Profile Picture -->
        <ImageView
            android:id="@+id/profile_image"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/ic_user"
            android:layout_marginBottom="8dp"
            android:scaleType="centerCrop"
            android:background="@drawable/circle_background" />

        <!-- Change Picture Text -->
        <TextView
            android:id="@+id/change_picture_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Change the Profile Picture"
            android:textSize="14sp"
            android:layout_marginBottom="15dp" />

        <!-- image upload not available -->
        <TextView
            android:id="@+id/imageUploadNotice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⚠️ Image upload is currently disabled in this version. You will get an error if u try upload an image "
            android:textColor="#FF0000"
            android:textSize="14sp"
            android:layout_marginTop="1dp"
            android:gravity="center"/>


        <!-- Form Label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Change Profile Settings"
            android:textSize="16sp"
            android:layout_marginBottom="12dp" />

        <!-- Username -->
        <EditText
            android:id="@+id/edit_username"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="#666666"
            android:hint="username"
            android:textColor="#ffffff"
            android:padding="10dp"
            android:layout_marginBottom="12dp" />

        <!-- Email -->
        <EditText
            android:id="@+id/edit_email"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="#666666"
            android:hint="Email"
            android:textColor="#ffffff"
            android:padding="10dp"
            android:layout_marginBottom="12dp"
            android:inputType="textEmailAddress" />

        <!-- Phone -->
        <EditText
            android:id="@+id/edit_phone"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:background="#666666"
            android:hint="Phone Number"
            android:textColor="#ffffff"
            android:padding="10dp"
            android:layout_marginBottom="32dp"
            android:inputType="phone" />

        <!-- Edit Button -->
        <Button
            android:id="@+id/btn_edit"
            android:layout_width="200dp"
            android:layout_height="48dp"
            android:text="Edit"
            android:backgroundTint="#4DB6E6"
            android:textColor="#ffffff"
            android:layout_gravity="center" />
    </LinearLayout>
</ScrollView>
